/**
 * Demo file for the exercise on binary tree
 *
 * @author Evgeny Hershkovitch Neiterman
 * @since 2023-03
 */

#include <iostream>
#include <fstream>
#include <sstream>
#include <stdexcept>
using namespace std;

#include "sources/Fraction.hpp"

using namespace ariel;


int main() {

    //Fraction{12345, 23456} * Fraction{34567, 45678}, Fraction{426920715, 1072143816};
    //  std::stringstream ss_in("1 2 3 -4");
    //  Fraction frac2, frac3;
    //  ss_in >> frac2 >> frac3;
    //  cout <<"frac2 :" <<frac2 << ". frac3 : "<<frac3 << endl;

    //  std::stringstream ss_in2("1 0");
    //  int a ,b;
    //  ss_in2 >> a >> b;
    //  cout << "a: " << a << " b: "<< b  << endl;

    // std::stringstream ss_zero_denominator;
    // ss_zero_denominator.str("6 8 3 0");
    // try
    // {
    //     ss_zero_denominator >> frac2 >> frac3;
    // }
    // catch(const std::runtime_error)
    // {
    //     cout << "succeed to catch" << endl;
    // }
    
    

    //cout << frac2 << endl << frac3 << endl;
    //cout <<  "b < a ? : " << (b<a) << endl;
    // Fraction a(5,3), b(14,21);
    // cout << "a: " << a << " b: " << b << endl;
    // cout << "a+b " << a+b << endl; 
    // cout << "a-b " << a-b << endl; 
    // cout << "a/b " << a/b << endl; 
    // cout << "a*b " << a*b << endl; 
    // cout << "2.3*b " << 2.3*b << endl; 
    // cout << "a+2.421 " << a+2.421 << endl; 
    
    // Fraction c = a+b-1;
    // cout << c++ << endl;
    // cout << --c << endl;

    // cout << "c >=b ? : " << (c >= b) << endl;
    // if (a > 1.1) cout << " a is bigger than 1.1" << endl;
    // else cout << " a is smaller than 1.1" << endl;


}
